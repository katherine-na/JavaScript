// Crear un Algoritmo que te convierta de minutos a segundos.

const converteMinToSg = (min) => {
  let seconds = 60;
  let result = min * seconds;
  return result;
};

console.log(converteMinToSg(2));
